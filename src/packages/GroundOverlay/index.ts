import { withInstall } from '../../utils/with-install'
import GroundOverlay from './GroundOverlay.vue'

export const ElBmapGroundOverlay = withInstall(GroundOverlay)
export default ElBmapGroundOverlay
